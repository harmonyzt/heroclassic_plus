public activate_ult(id) {
    if(is_user_alive(id) && is_user_connected(id))
    switch(msm_get_user_hero(id)){
        case NONE:
        {
            
        }
        case SL:{
            
        }
        case UNDYING:
        {
        }
        case BERSERK:
        {
            
        } 
        case ZEUS:
        {
        }
    }
}